apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-app
spec:
  replicas: 2
  selector:
    matchLables:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec: 
      containers:
      - name: depoy-container
        image: thangamec0312/todayimage
        ports:
        -  containerPort: 80
